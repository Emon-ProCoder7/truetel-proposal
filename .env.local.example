# Paste your n8n Webhook node's Production URL here.
# Method: POST · Response mode: "Immediately" (the app doesn't wait on n8n
# to finish rendering/sending — it fires the payload and shows a confirmation).
# See README.md "n8n contract" for the exact JSON body shape.
NEXT_PUBLIC_N8N_WEBHOOK_URL=https://your-n8n-instance.example.com/webhook/cloud-phone-proposal
